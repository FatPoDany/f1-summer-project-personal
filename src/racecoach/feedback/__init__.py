"""Run-level LLM feedback. The contract is fixed and the validator enforces
the project's red line: every issue must cite evidence that exists in the
run's metrics JSON — an untraceable claim is a schema error, not a style
problem."""
