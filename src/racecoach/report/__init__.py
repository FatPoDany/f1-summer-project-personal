"""Self-contained HTML run reports — metrics, charts with event annotations,
and the validated feedback, in one file that travels (blog, status forms,
the final write-up). No external assets, same dark language as the app."""
