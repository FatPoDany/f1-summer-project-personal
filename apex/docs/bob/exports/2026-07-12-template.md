---
tool: IBM Bob (Bob IDE)
date: 2026-07-12
scope: TEMPLATE — replace with the files/modules Bob was shown
prompt: TEMPLATE — replace with the question asked in Bob
---

# TEMPLATE — not evidence

This file only documents the export format (see ../README.md). Real exports
are named `YYYY-MM-DD-<topic>.md`, carry the header above with real values,
and contain Bob's actual answer below it. Files with "template" in the name
are ignored by `racecoach coach --bob`.
