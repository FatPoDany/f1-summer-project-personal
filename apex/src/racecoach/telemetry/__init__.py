"""Telemetry capture and storage.

Path A (current): import CSVs produced by the team's simuv2 exporter.
Path B (pending environment): a live SCR-client logger will write the same
run layout, so everything downstream stays capture-agnostic.
"""
